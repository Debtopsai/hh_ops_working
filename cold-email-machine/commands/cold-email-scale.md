---
description: Calculate the infrastructure needed to hit a target call volume, and check readiness to scale
---

Run `cold-email-ops` scaling plus `cold-email-infrastructure` capacity planning.

Target: $ARGUMENTS

First confirm the system is optimised and J (emails per booked call) is measured, not guessed. If J is unknown, say so and define how to measure it before doing any maths. Then apply the scaling equation, check sales capacity absorbs the call volume, and flag the four common scaling failures: reply buildup, lead mismanagement, lists running out, copy burnout.
