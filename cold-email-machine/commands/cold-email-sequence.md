---
description: Write a full cold email sequence with spintax and an A/B variant matrix
---

Run the `cold-email-copywriting` skill.

Brief: $ARGUMENTS

Read the campaign profile if one exists, and `${CLAUDE_PLUGIN_ROOT}/shared/compliance.md` to determine whether an opt-out path is legally required for this market. Produce a 3-email sequence using the triple tap, with spintax inline, all permutations rendered and read, a spam-word check on the permutations, and an A/B matrix that isolates one element per layer.
